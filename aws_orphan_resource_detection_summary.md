# AWS 放置リソース検知 調査結果まとめ

| 項目 | 内容 |
|---|---|
| 版 | 0.4（2026-09-24） |
| 目的 | プロジェクト消滅後に残る放置リソースをどう検知できるかの調査結果。観点ごとの判定条件と検知できるもの／できないもの、検知手段の評価、機械的に判定できない範囲、検知後の扱い方をまとめる |
| 併読 | `aws_orphan_detection_initial_plan.xlsx`（初期導入の推奨案：概算コスト・導入工数・限界。入力値を変えると再計算される） |
| 基準スケール | 1 アカウント／リソース約 5,000／CloudTrail 管理イベント 500 万件/月／IAM ロール 500／S3 オブジェクト 1 億／月間 AWS 利用額 $20K |
| 不変条件 | 自動停止・自動削除は行わない（§4.1） |

---

## 1. 観点別の判定条件と、検知できるもの／できないもの

共通の前提：判定単位はリソース ARN（CUR の resource_id は ARN に正規化）。各条件の出力は true／false／unknown で、unknown は休眠の証拠にしない。条件は「候補化」の材料であり、単独で処置を決めない。放置は「休眠（使われていない）＝観点 A／B／C／E」と「無主（責任者・PJ が消滅）＝観点 D」に分けて扱い、無主だが稼働中のものは D でしか捕まらない。

### 1.1 観点 A：一定期間、コスト額が変動しない

データソース：Data Exports（CUR 2.0）を Athena でリソース×日に集計。Cost Explorer はリソース粒度が過去 14 日のみで、W≥30 の判定には使えない。判定は金額ではなく使用量（usage_amount）で行い、Savings Plans／RI 按分の揺れを排除する。Fee／Refund／Credit／Tax の行と月額一括計上の usage_type は除外する。

| 条件 | 判定条件（パラメータ） | 検知できるもの | 検知できないもの |
|---|---|---|---|
| A-1 コスト一定 | 直近 W_A 日の日次使用量について `(max − min) / mean ≤ Δ_A`、かつ日額平均 ≥ c_min、かつデータ存在日比率 ≥ cov_min | 時間課金で放置されている常時稼働リソース（止め忘れ EC2／RDS／ElastiCache／OpenSearch／SageMaker エンドポイント、NAT GW、ALB／NLB、EIP、EBS、プロビジョンド DynamoDB、Kinesis シャード、WorkSpaces、VPN／TGW アタッチメント）。容量が増えないストレージ（放置スナップショット、静的バケット） | 固定料金で**使用中でも**フラットなもの（Route 53 ゾーン、KMS キー、Secrets、アラーム）→ B と AND が必須。スケジュール処理が残って動いている無主リソース（コストが動く）→ D。CUR に resource_id が付かない行（データ転送の一部、Support、税）。CUR 作成前の期間（W_A 分の蓄積が要る） |
| A-2 ほぼ無料 | 日額平均 < c_min | 「A では判定できない」リソースの振り分け（IAM、SG、アイドルの Lambda／API GW／SQS／オンデマンド DynamoDB、空バケット）→ B／E で判定する | 放置かどうかそのもの（金額が無いので変動も無い） |
| A-3 単調増加 | ストレージ系の使用量について非減少日比率 ≥ mono_min、かつ 30 日増分 ≥ g_min | 書かれ続けているログ蓄積バケット／LogGroup／Backup／スナップショット増分。B-4（読み出し 0）と組み合わせて「書かれるが読まれない」を候補化 | 増分が小さく Δ_A 内に収まるもの（A-1 側で拾う）。読まれているかどうか（B が必要） |
| A-4 急減後フラット | 過去 W_A 日より前に使用量が k 分の 1 以下に減少し、その後 A-1 を満たす | PJ 終了直後に残った基盤（利用が止まって費用が固定化したもの）。転換点から終了時期を推定できる | 元々一定だったもの（A-1 と同じ）。転換点より前の CUR 履歴が無い場合 |

| パラメータ | 初期値 | 説明 |
|---|---|---|
| W_A（窓） | 60 日 | 月次バッチと月末処理を 2 周期含める。長いほど CUR の蓄積待ちが延びる |
| Δ_A（許容変動幅） | 3% | 秒課金の丸めと日数差で ±1〜2% は揺れる |
| c_min（下限日額） | $0.10/日 | 足切り。これ未満は A-2 として B／E に回す |
| cov_min（データ存在日比率） | 95% | CUR の 24〜48 時間遅延・欠損に耐える |
| mono_min／g_min | 90%／5% | A-3 用 |
| k（急減倍率） | 3 | A-4 用 |

### 1.2 観点 B：一定期間、API コール／アクセスが発生していない

データソース：CloudWatch メトリクス（1 時間粒度が 455 日保持されるので W=90 を遡れる）、サービス固有の最終利用属性（無料の Describe／Get API）、CloudTrail 管理イベント（90 日イベント履歴または既存トレイル）。

| 条件 | 判定条件（パラメータ） | 検知できるもの | 検知できないもの |
|---|---|---|---|
| B-1 データプレーンの無利用 | タイプ別の利用メトリクスの直近 W_B 日の集計が τ_B 以下。EC2：CPU Max<5% かつ NW<5MB/日、EBS：<1 IOPS/日、Lambda／DynamoDB／SQS／SNS／Kinesis／API GW／Step Functions／SageMaker：0、RDS／Redshift／ElastiCache／EFS：接続 0、ALB／NLB：ターゲット到達 2xx＝0、NAT：接続 0、LogGroup：IncomingBytes 0、Route 53：DNSQueries 0 | 固定料金・低コストでも使われていないもの（A の死角）。従量課金の休眠（Lambda、SQS、DynamoDB）。実利用の無いエンドポイント | S3 のオブジェクトアクセス（データイベント／サーバーアクセスログ／Storage Lens advanced のいずれも無いと不可）→ 初期は unknown。メトリクスの無いタイプ（SG、VPC、IAM ポリシー、SSM）→ B-2。公開エンドポイントへのボット流入、常駐コネクションプール、OS のバックグラウンド IO で「使用中」に見えるもの。年次バッチ（W_B=90 では休眠と誤判定）→ 除外タグ |
| B-2 コントロールプレーンの無参照 | CloudTrail 管理イベント（読み取り含む）のうち、除外プリンシパル以外による対象リソースの参照が W_B 日間 0 件 | メトリクスの無いタイプ（SG、VPC／サブネット、IAM ポリシー、SSM パラメータ、Glue カタログ、KMS キー） | 棚卸しツール・監視・Config が定期的に Describe するため常に「参照あり」に見えるもの（除外プリンシパル一覧が必須）。resources が空のイベント。90 日以前（既存トレイルが無い場合） |
| B-3 最終利用時刻が古い | サービス固有の最終利用時刻から W_B 日以上（IAM `RoleLastUsed`、Secrets `LastAccessedDate`、ECR `lastRecordedPullTime`、WorkSpaces 最終接続時刻） | IAM ロール／ユーザー・アクセスキー、Secrets、ECR、WorkSpaces の休眠 | 属性を持たないタイプ。属性の粒度が粗いもの（Secrets は日単位、IAM は 400 日で打ち切り） |
| B-4 書かれるが読まれない | 書き込み系メトリクス > 0 かつ読み出し系メトリクス = 0（SQS 送信>0 受信 0、S3 Put>0 Get 0、LogGroup 書込>0 でクエリ実績なし） | 無主で動き続けるジョブの産出物（誰も消費していない） | 読み出し側のメトリクスが無いタイプ（S3 は Storage Lens／アクセスログが必要）。上流ジョブそのもの（D でしか捕まらない） |

| パラメータ | 初期値 | 説明 |
|---|---|---|
| W_B（窓） | 90 日 | 四半期バッチを含める。年次バッチは捕捉できない |
| τ_B（タイプ別閾値） | 上表 | Compute Optimizer のアイドル基準に整合。公開エンドポイントは「ターゲット到達 2xx」で判定 |
| 除外プリンシパル | AWS サービスロール、Control Tower、Config／Security Hub／GuardDuty／Backup／SSM、監視 SaaS、棚卸しツール | 「生存反応」を除く。四半期ごとに見直す |
| データ完全性 | 窓の 90% | 欠損時は unknown |

### 1.3 観点 C：一定期間、リソースの更新（最終変更日時）がない

データソース：AWS Config の `configurationItemCaptureTime`（既に有効な場合のみ）、CloudTrail の書き込み系管理イベント（readOnly=false）、サービス固有の最終変更属性。

| 条件 | 判定条件（パラメータ） | 検知できるもの | 検知できないもの |
|---|---|---|---|
| C-1 Config の最終構成変更が古い | `configurationItemCaptureTime` から W_C 日以上（Config 有効化時の初回キャプチャは変更と見なさない） | Config 対応タイプの構成無変更。リレーション（何に接続されているか）も同時に取れる | Config 未導入の環境。有効化前の変更。データプレーンの更新（S3 オブジェクト、DynamoDB 項目）。Config 対象外タイプ |
| C-2 書き込みイベントが無い | CloudTrail の書き込みイベントのうち、除外プリンシパル以外によるものが W_C 日間 0 件 | Config が無くても判定できる。既存トレイルがあれば年単位で遡れる | 自動更新（ASG 入替、証明書更新、バックアップ、ローテーション）で「触られた」ように見えるもの。90 日以前（トレイルが無い場合）。resources 欠落イベント |
| C-3 最終変更属性が古い | CloudFormation スタック `LastUpdatedTime`、Lambda `LastModified`、SSM／Secrets の更新日、ECR push 日、AMI／スナップショット作成日から W_C 日以上 | CDK／CFN 管理分はスタック単位で一括判定できる（最短経路）。Lambda、SSM、ECR | 属性の無いタイプ。CFN 外のリソースにはスタックの近道が使えない |
| C-4 作成後一度も変更なし | 作成日 ＝ 最終変更日、かつ作成から W_C 日以上 | 作って放置された試作・検証リソース | 意図的に不変な本番 |

観点 C 共通の「検知できない」：安定稼働している本番は無変更が正常なので、C 単独では放置の証拠にならない。必ず B と AND で使う。

| パラメータ | 初期値 | 説明 |
|---|---|---|
| W_C（窓） | 120 日 | 本番の安定運用と区別するため長め |
| 除外プリンシパル | B と共通 | 自動更新を除く |

### 1.4 観点 D：タグの Owner が在籍していない／PJ 終了宣言済み

データソース：Resource Explorer／Resource Groups Tagging API（タグ値）、在籍マスタ（IAM Identity Center、Entra ID、人事 DB）、PJ 台帳（無ければスプレッドシートで代用）、Organizations のアカウントタグ・代替連絡先。

| 条件 | 判定条件（パラメータ） | 検知できるもの | 検知できないもの |
|---|---|---|---|
| D-1 Owner 不在 | Owner タグ値を正規化（小文字化、表示名→メール）して在籍マスタと照合し、退職・無効、または退職から grace_D 日以上 | Owner が退職・異動で不在のリソース（**稼働中でも捕まえられる唯一の条件**） | タグ未付与（母集団外）。自由記述の Owner 値（チーム名・略称・typo）。Owner は在籍しているが関心を失っている。引き継ぎ済みでタグ未更新（誤検知） |
| D-2 PJ 終了 | Project タグ値が PJ 台帳で「終了」（一時停止は除く） | PJ 終了に紐づくリソース一式（稼働中でも） | PJ 台帳が無い組織。一時停止と終了の区別。台帳と実態のずれ |
| D-3 代替 Owner 不在 | Owner タグが無い場合、CloudFormation スタックのタグ → Organizations のアカウント Owner／代替連絡先の順で解決し、D-1 相当を判定 | タグ未付与でも CFN 管理分、または「アカウント＝PJ」構成なら判定できる | CFN 外かつアカウントが混在利用のもの。アカウント Owner が在籍していれば個々の放置は見えない |
| D-4 未タグ | Owner／Project タグがどちらも無い（Resource Explorer の `tag:none`） | 未タグの存在そのもの（母集団外の列挙、タグ付与率の把握） | 放置かどうか |

| パラメータ | 初期値 | 説明 |
|---|---|---|
| Owner タグキー候補 | Owner, owner, OwnerEmail | チーム名は人ではないので含めない |
| Project タグキー候補 | Project, ProjectCode | Cost Categories を台帳にするならその名前 |
| grace_D（退職後猶予） | 30 日 | 引き継ぎ期間 |
| 解決順序 | タグ → スタック → アカウント | 確度は 1.0／0.7／0.5 として扱う |

### 1.5 観点 E（補助）：構造的に孤立している

データソース：各サービスの Describe API、Compute Optimizer／Cost Optimization Hub、Trusted Advisor（Business Support+ 以上を契約している場合）。PJ 状態に関わらず削除候補になり、タグ非依存で既存リソースに即効く。

| 条件 | 判定条件（W_E） | 検知できるもの | 検知できないもの |
|---|---|---|---|
| E-1 接続・関連付けが無い | 未接続 EBS（32 日）、未関連付け EIP（7 日）、未接続 ENI・どの ENI にも付かない SG（30 日）、`InUseBy` が空の ACM 証明書（30 日）、ターゲット 0 の ELB／ターゲットグループ（30 日）、ルートテーブル未参照の NAT GW（30 日）、ENI 0 の VPC（30 日） | 使われていないことが構造的に確定するもの | 構造上は接続されているが無主のもの（放置の大半。A〜D で扱う） |
| E-2 参照元が消えている | 元ボリューム消失後のスナップショット（180 日）、未参照の AMI・起動テンプレート（90〜180 日）、pull されない ECR（180 日）、ポリシー未アタッチの IAM ロール（180 日） | 削除漏れの派生物 | 外部アカウント・コード内から参照されているもの |
| E-3 停止状態が続いている | 停止 30 日超の EC2、Stopped の SageMaker notebook（90 日）、retention 未設定で書込 0 の LogGroup（90 日） | 止めたまま忘れられたもの | 意図的な停止（DR、季節利用）→ 除外タグ |
| E-4 AWS のアイドル推奨 | Compute Optimizer のアイドル推奨（EC2、ASG、EBS、ECS Fargate、RDS／Aurora、NAT GW、DynamoDB、ElastiCache、MemoryDB、DocumentDB、WorkSpaces、SageMaker エンドポイント）、Trusted Advisor のアイドル系チェック（契約時） | 12 タイプの利用率ベースのアイドル。AWS 定義の閾値で説明しやすい | ルックバックが 14〜63 日で W=90 を単独では満たさない（推奨エクスポートを蓄積して連続日数を数える）。対象外タイプ |

### 1.6 組み合わせ（候補の Tier）

| Tier | 判定 | 意味 | 処置方針（人が実施） |
|---|---|---|---|
| T1 構造的孤立 | E-1〜E-4 のいずれか | 使われていないことが構造的に確定 | 30 日猶予後に削除候補 |
| T2 休眠 | (A-1 ∨ A-2) ∧ (B-1 ∨ B-3) ∧ (C-1〜C-4 のいずれか) | 使われず・触られず・費用も動かない | Owner 確認→隔離→削除候補 |
| T3 書かれるが読まれない | A-3 ∧ B-4 | 産出物を誰も消費していない | 上流ジョブの要否確認 |
| T4 無主 | D-1 ∨ D-2 ∨ D-3（稼働の有無を問わない） | 責任者／PJ 不在 | 後任確認。休眠なら T2 と同じ |
| T5 要確認 | 上記に該当せず、または unknown が 2 観点以上 | 証拠不足 | 通知しない。翌月再判定 |

確度は `1 − 0.25 × unknown の観点数`（T2 は 0.5 以上のときのみ通知）、優先度は月額 × Tier 重み。同一 CloudFormation スタック内はスタック単位に丸めて 1 候補にする。

### 1.7 パラメータ一覧（初期値）

| 記号 | 観点 | 初期値 |
|---|---|---|
| W_A／Δ_A／c_min／cov_min／mono_min／g_min／k | A | 60 日／3%／$0.10 日／95%／90%／5%／3 |
| W_B／τ_B／データ完全性 | B | 90 日／タイプ別（§1.2）／90% |
| W_C | C | 120 日 |
| grace_D | D | 30 日 |
| W_E | E | 7〜180 日（タイプ別、§1.5） |
| 高額エスカレーション閾値 | 運用 | $100/月 |
| 除外の既定期限／上限 | 運用 | 90 日／365 日 |
| リマインド | 運用 | 30 日、60 日、90 日でエスカレーション |

---

## 2. 検知手段の評価

| 手段 | 対応観点 | 有効化前履歴（遡れる範囲） | タグ非依存／CDK 外 | 制約・誤検知要因 |
|---|---|---|---|---|
| Data Exports（CUR 2.0）+ Athena | A（本命） | N（作成時点以降。W_A=60 は約 2 か月後から） | Y／Y | 月額一括計上、SP／RI 按分、resource_id 欠落行 |
| Cost Explorer API | A（補助） | Y（月次 38 か月、日次 14 か月。リソース粒度は 14 日のみ） | Y／Y | リソース粒度 14 日で W_A を満たさない。リクエスト課金 |
| Cost Anomaly Detection | — | — | Y／Y | 支出「増加」の検知器。「変動なし」は判定不能（用途外） |
| CloudTrail 90 日イベント履歴 | B、C | Y（90 日分） | Y／Y | LookupEvents は 2 TPS、属性検索が粗い。90 日以前は不可 |
| CloudTrail 既存トレイル + Athena | B、C、D（作成者推定） | 条件付き（トレイルがあれば開始時点以降） | Y／Y | 監視・自動化の読み取り、resources 欠落イベント。トレイルが無ければ今から作成し蓄積待ち |
| CloudTrail Lake | B、C | 条件付き（S3 のログを取り込めば） | Y／Y | 非圧縮 GB 課金。S3＋Athena と情報が重複 |
| CloudTrail データイベント | B（S3／Lambda の最終アクセス） | N（有効化以降） | Y／Y | 件数課金（10 万件あたり $0.10）で S3／Lambda の呼び出し規模に比例して高額化。無料メトリクスで代替可 |
| AWS Config（既に有効） | C（本命）、E、D | N（有効化以降） | Y／Y | 有効化時の初回キャプチャを変更と誤認、リレーション更新。対象外タイプあり |
| AWS Config（新規有効化） | C、E、D | N（有効化以降） | Y／Y | 有効化前の履歴は無い。CI 数（変更頻度）に比例して課金 |
| Config ルール（周期評価） | E、D | N | Y／Y | 評価数（リソース数 × 日数）に比例して課金。advanced query で代替可 |
| Resource Explorer／Tag Editor／Tagging API | D（母集団）、B／C の列挙元 | —（現在の状態のみ） | Y（`tag:none` で未タグ列挙）／Y | 索引対象外タイプ。利用状況・時刻は持たない。索引構築に最大 36 時間 |
| Trusted Advisor | E、B（限定） | Y（チェック既定の 7〜30 日） | Y／Y | Business Support+ 以上の契約が必要。Idle LB は Classic のみ等。大半は Cost Optimization Hub の再掲 |
| Compute Optimizer + Cost Optimization Hub | E、B | Y（既定 14 日、EBS／NAT は 32 日、WorkSpaces は 63 日） | Y／Y | ルックバックが短く W=90 を単独では満たさない。対象 12 タイプ。オプトイン後 24 時間で反映 |
| CloudWatch メトリクス（GetMetricData） | B（本命）、E | Y（1 時間粒度で 455 日） | Y／Y | ボット流入、常駐接続、監視の生存反応。S3 は既定では取得不可 |
| サービス固有の最終利用／変更属性 | B、C、E | Y（属性依存。IAM は 400 日） | Y／Y | 属性の無いタイプ。粒度が粗いもの（Secrets は日単位） |
| S3 Storage Lens advanced | B（S3） | N（有効化以降、15 か月保持） | Y／Y | オブジェクト数に比例して課金。反映まで 48 時間 |
| S3 リクエストメトリクス | B（S3） | N（有効化以降） | Y／Y | バケットごとにカスタムメトリクス課金（約 16 メトリクス/バケット） |
| IAM Access Analyzer 未使用アクセス | B（IAM） | N（90 日） | Y／Y | ロール数に比例して課金。`RoleLastUsed`（無料）と情報が重複 |

---

## 3. 機械的に判定できない範囲

| # | 事象 | 機械化できる範囲 | 人が担う部分 | 緩和策 |
|---|---|---|---|---|
| 1 | PJ が本当に終了したか | PJ 台帳があれば照合 | 終了宣言そのもの、一時停止との区別 | PJ 台帳（スプレッドシート可）を作り Project タグ／Cost Categories と紐づける |
| 2 | 休眠と待機の区別（DR スタンバイ、季節／年次バッチ、監査・法令保管、Glacier／Backup） | 候補化まで | 待機であることの宣言 | 期限付き除外タグ（理由コード付き） |
| 3 | 共有・基盤リソース（VPC、TGW、KMS キー、共用 IAM ロール、ログバケット）の孤立 | Config リレーション、ACM `InUseBy`、ENI 参照による部分判定 | 外部システム・他アカウント・コード内参照の確認 | 除外台帳のパターンで扱い年次レビュー |
| 4 | 無主だが稼働中（EventBridge／cron で動き続けるジョブ） | B-4（書かれるが読まれない）で候補化 | 産出物の消費者の有無 | 通知に上流ジョブの Owner 確認を含める |
| 5 | Owner の意味解決（後任、組織改編、委託先、チームアドレス） | 在籍マスタ照合 | 誰が引き継いだか | 通知に「Owner 更新」の依頼を含め、上長へフォールバック |
| 6 | Owner は在籍しているが関心を失っている | 検知不能 | Owner 本人の判断 | T2／T3 の通知で判断を迫る |
| 7 | 削除可否と影響範囲（データ保持義務、復元可能性） | 依存関係の一覧化 | 削除判断、承認 | 処置は「隔離→猶予→削除」の 3 段階。自動化しない |
| 8 | 列挙元のカバレッジ外のタイプ | Resource Explorer／CUR／CloudTrail の 3 系統をクロスチェックし差分を出力 | 差分の確認 | 四半期ごとに対応タイプを更新 |
| 9 | ボット流入、常駐コネクション、OS の背景 IO | 閾値調整、ターゲット 2xx 判定 | 実利用かどうか | 通知にメトリクスグラフの URL を添付 |
| 10 | 年次バッチ（W_B=90 で捕捉不能） | 検知不能 | 事前宣言 | SEASONAL 除外タグ（期限＝次回実行日＋30 日） |
| 11 | タグ未付与既存リソースの Owner | 作成者（CloudTrail の Create イベント）、スタック、アカウントの順で推定 | 作成者≠Owner の補正、CI/CD ロール経由の追跡 | 新規は SCP／タグポリシーで強制。既存はアカウント粒度で運用 |
| 12 | 一時的にアイドルな正当リソース（負荷試験環境、四半期末処理） | 候補化まで | 用途の説明 | 除外（理由 OTHER＋コメント、期限 90 日） |

---

## 4. 検知後の扱い方（方針）

### 4.1 不変条件：自動停止・自動削除は行わない

| 自動化する | 自動化しない（人が承認し、人が実行） |
|---|---|
| データ収集・判定・集計、候補一覧の生成と保存 | 停止（EC2／RDS の stop、Lambda 同時実行 0） |
| 通知（Slack／Teams、メール、チケット起票） | 隔離（Deny ポリシー付与、SG 付け替え、ライフサイクル設定） |
| 候補タグ `od:*`（状態・初回検知日・Tier）の付与 | 削除・解放・登録解除 |
| 除外期限の監視と判定対象への復帰 | 除外の承認、Owner の確定、PJ 終了の宣言 |

「猶予期間経過後の自動削除」「Tier1 は自動解放」「タグで opt-in したものは自動削除」といった例外も設けない。検知処理の IAM ロールは読み取りと `od:*` タグ付与のみを許可し、Delete／Terminate／Stop／Release／Deregister／Modify 系を明示 Deny して、誤って自動削除される状態を設定上あり得なくする。判定結果は「候補」であり削除の根拠ではなく、削除の根拠は人の確認記録（チケット）とする。

### 4.2 通知先

| 順位 | 条件 | 通知先 |
|---|---|---|
| 1 | Owner タグが解決でき在籍 | Owner 本人（メール）＋Owner のチームチャネル |
| 2 | Owner 不在または未解決 | CloudFormation スタックの Owner → アカウントの Owner（Organizations タグ／代替連絡先）。「Owner 更新」の依頼を必ず含める |
| 3 | いずれも不在 | 既定の受け皿（プラットフォーム／FinOps 担当チャネル）。受け皿は必ず 1 つ定義し、空振り通知を作らない |
| 追加 | 月額 ≥ $100 | FinOps 担当と部門責任者にも通知 |
| 追加 | セキュリティ関連（IAM ロール、公開 SG、公開バケット） | セキュリティ担当にも通知 |
| 全体 | 毎月の実行結果 | サマリチャネル（アカウント別・Tier 別の件数と金額） |

個別通知はチケット（1 候補または 1 スタック＝1 チケット）を正とし、チャットとメールはチケットへのリンクを配る。通知には対象一覧、月額、判定根拠、対応期限、除外方法を含める。

### 4.3 通知頻度・抑制

| 種類 | 頻度 |
|---|---|
| 月次サマリ＋新規候補の個別通知 | 毎月第 1 営業週に 1 回（T5 は通知しない） |
| リマインド | 未対応なら 30 日後、60 日後に同じチケットへ追記 |
| エスカレーション | 90 日無反応で上長／アカウント Owner へ（隔離提案を添える。実施は人） |
| 高額（≥ $100/月）または T1 | 週次リマインド |
| 除外期限 | 期限 14 日前に除外者へ。期限切れ当日に判定対象へ復帰し、翌月再通知 |

抑制：同一リソース・同一 Tier・同一状態の再通知はしない（状態変化、金額 50% 以上の変動、期限到来のときのみ）。1 通知の掲載は 50 件まで（超過分は CSV 添付）。同一スタック内は 1 件に丸める。営業日の日中に送る。

### 4.4 誤検知の除外方法

- **除外タグ**（タグ付与可能なタイプ）：`od:exclude=true`、`od:reason`（DR_STANDBY／SEASONAL／ARCHIVE_RETENTION／COMPLIANCE_HOLD／SHARED_PLATFORM／IN_DECOMMISSION／TEMP_WORKLOAD／OTHER）、`od:until=YYYY-MM-DD`、`od:by=承認者` を必須にする。既定 90 日、上限 365 日。
- **除外台帳**（タグ付与不可のタイプ、パターン除外、基盤リソース）：ARN またはパターン、理由、承認者、期限、チケットを CSV で管理し、変更はレビュー必須。パターン除外も期限必須。
- **除外プリンシパル一覧**（観点 B／C の生存反応の除去）：AWS サービスロール、Control Tower、監視・バックアップ・スキャナ・棚卸しツールのロール。四半期ごとに見直す。
- **期限管理と監査**：期限切れは自動で判定対象に復帰。延長は再承認。月次サマリに除外中の一覧（件数・金額・理由別）と期限切れ予定を載せる。
- **フィードバック**：除外理由を月次集計し、SEASONAL／TEMP_WORKLOAD が多いタイプは閾値・窓の調整候補、SHARED_PLATFORM が多ければ列挙元のフィルタ候補にする。除外率が 30% を超えたらパラメータを見直す。

### 4.5 処置の流れと確認指標

処置は人が「チケットに判定根拠・承認者・バックアップの所在・猶予期限を記録 → バックアップ／スナップショット → 停止または隔離 → 猶予 30 日 → 削除」の順で行う。確認指標は、候補件数・候補月額（Tier 別）、通知から処置または除外までの中央値日数、除外率、削除実績（月額）、未タグ率の 5 つで足りる。
