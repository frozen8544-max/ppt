# AWSタグ（Unit / Project / Env / Owner）変更に関する調査結果

## 背景

- 運用中のタグ：Unit / Project / Env / Owner の4種
- Owner の値：PJ責任者メールアドレスの「@」より前を使用

## 結論（一言）

**キー名は変えない。集計軸は不変IDタグにし、表示名（Unit / Owner 等）の変更は Cost Categories で旧値・新値を束ねて吸収する。**

---

## 調査項目1：タグ「キー名」を変更した場合

### コスト配分タグの有効化状態

- 旧キーの履歴は旧キー名のまま残る。
- 新キーは別タグとして扱われ、管理アカウントで再度有効化が必要。
- タグ一覧への出現まで最大24時間、有効化後の反映までさらに最大24時間かかる。
- バックフィルは最大12か月遡れるが、その期間にリソースへ実際に付いていたタグしか反映されないため、新キーで旧期間は埋まらない。

### Cost Explorer / Budgets / Cost Categories での参照

- Cost Explorer と Budgets では旧キー・新キーは別ディメンションとなり、自動では繋がらない（フィルタに出すには有効化が必須）。
- Cost Categories で「旧キー=X または 新キー=X」を同一値にまとめれば繋がる。ルールは過去12か月の任意の月から遡及適用できる。
- Budgets のフィルタ、Cost Explorer の保存レポート、異常検知モニターは作り直しが必要。

### IAM ポリシーの ABAC 条件への影響

- 条件はキー名で照合するため、変更した瞬間に `aws:ResourceTag` / `aws:PrincipalTag` / `aws:RequestTag` / `aws:TagKeys` が不一致になり権限が外れる（Allow も Deny も効かなくなる）。
- 移行中は両キーを許容するポリシーを先に配布する。
- 条件キー名は大文字小文字を区別しないため、大小文字だけの変更であれば影響はない。

---

## 調査項目2：タグ「値」を変更した場合

### Unit の値を変更した場合

- Cost Explorer 上では旧値と新値が別行になり、自動では同一対象にならない。
- Cost Categories で両値を1つのカテゴリ値にマッピングすれば追跡可能（12か月遡及）。
- バックフィルは値の書き換え機能ではないため使えない。

### Owner の値を変更した場合

- 通知先をタグ値から導出する仕組みの場合、即座に新責任者へ切り替わる（旧責任者には届かなくなる）。
- コスト履歴は旧値のまま残り、Owner で集計すると分断される。
- 個人メール由来の値は、AWS がタグに PII を入れないよう求めている点でも不利。Owner は集計軸にせず、役割名・配信リスト名にするのが安全。

---

## 調査項目3：不変の識別子を持たせられるか

### 不変の識別番号を別タグとして持たせる案

- 実現可能。例：`ProjectId`（採番 / UUID）を別タグで付与し、これもコスト配分タグとして有効化する。
- 集計軸は ID タグ。表示名タグ（Unit / Project）は人向けメタデータとして扱う。
- ID を表示名に変換して見せる役は Cost Categories が担う。

### Cost Categories によるマッピングのみでの代替

- Cost Explorer・Budgets・CUR・異常検知に追加ディメンションとして現れるため、請求系では代替可能。
- ただし IAM ABAC や Config / Backup 等の「請求以外のタグ利用」には効かず、変更のたびにルール保守が必要。
- したがって主軸は ID タグ、補助として Cost Categories を使う。

---

## 調査項目4：変更をどう実行するか

### 対象リソースの洗い出し

- **Resource Explorer**：主に使う。アグリゲータリージョンを設定すればマルチアカウント・マルチリージョンでタグ検索できる。Tag Editor の機能は Resource Explorer コンソール側に移されている。
- **Tag Editor**：1アカウント内の検索・一括編集。
- **Resource Groups**：タグクエリの保存・再利用向け。
- 課金対象の補完として CUR も併用する。

### 一括変更の手段と稼働中サービスへの影響

- Tag Editor は最大500リソースまで一括操作可能。キー自体の編集はできず、削除して新しいキーで作り直す必要がある。
- 大量の場合は Resource Groups Tagging API でスクリプト化する。
- タグはメタデータのため、リソース自体は停止・再起動しない。
- 副作用：
  - ABAC 権限の一時喪失
  - タグ依存の自動化（Backup 選択、SSM ターゲット、ASG / EMR からの伝播 等）
  - タグポリシーの `enforced_for` による操作拒否
- 実施順序は「新キー追加 → 確認 → 旧キー削除」とする。

### CloudFormation / CDK 管理下のリソース

- コード側を変更してスタック更新が必須。
- タグはリソース属性として CloudFormation が追跡するため、手動変更はドリフトとして検出され、次回デプロイで元に戻される。
- タグ変更は大半のリソースで「No interruption」だが、種別によっては置換が発生する。
  - 例：`AWS::Batch::JobQueue` の Tags は Replacement
  - 例：Batch ComputeEnvironment の `ComputeResources.Tags` は置換なしに変更できない
- `cdk diff` / 変更セットで Replacement の有無を必ず確認し、該当リソースはデータ退避・DeletionPolicy を先に手当てする。
- スタックレベルのタグは配下リソースへ伝播するため、影響範囲が広い点に注意。

### CDK 管理外の手動作成リソース

- Tag Editor / Tagging API で変更し、台帳に記録する。
- 将来的には `cdk import` で IaC 配下に入れるか、タグポリシー＋Config `required-tags` で継続検出する。

---

## 調査項目5：運用ルールとしてどう定めるか

### タグ名・タグ値の変更を認めるか

- **キー名変更**：原則禁止。タグポリシーでキー名・大小文字・許容値を固定する。
- **値変更**：
  - Unit / Project / Env は原則不変（ID タグを併用）
  - Owner は人事異動に伴う変更を通常手続きとして許可。ただし集計軸にしない。

### 変更が避けられない場合の手順

1. **申請**：Resource Explorer で対象一覧をエクスポートし、影響する IAM ポリシー / Budgets / Cost Explorer レポート / Cost Categories / 自動化を列挙する。
2. **影響評価**：変更セットで置換の有無を確認する。
3. **承認**：PJ責任者＋FinOps＋セキュリティ。
4. **実施**：IAM 両キー対応 → 新タグ有効化 → Cost Categories 更新 → CloudFormation 更新 → 手動リソース更新 → 旧キー削除。
5. **記録**：変更チケット＋CloudTrail の TagResource / UntagResource イベント。

### 変更後に必要な確認項目

- 新タグのコスト配分有効化と、24〜48時間後の Cost Explorer への反映確認（必要ならバックフィル）
- Budgets / 保存レポート / 異常検知 / Cost Categories の更新と「適用済み」ステータスの確認
- IAM の疎通確認（Policy Simulator＋実操作）
- 旧キーの残存ゼロ確認（Resource Explorer / タグポリシー準拠レポート）
- 台帳の更新
