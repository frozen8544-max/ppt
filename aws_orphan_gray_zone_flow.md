# 機械的に判断できない範囲の扱い：グレー判定 → ヒアリング → 削除可否 フロー

| 項目 | 内容 |
|---|---|
| 版 | 0.1（2026-09-24） |
| 位置づけ | 調査結果まとめ §3「機械的に判定できない範囲」の 12 事象を、処置の流れとして整理したもの。図中の #1〜#12 は §3 の番号 |
| 不変条件 | どの経路でも自動停止・自動削除は行わない。停止・隔離・削除は人が承認し、人が実行する |

画像：`aws_orphan_gray_zone_flow.png`（PNG）／`aws_orphan_gray_zone_flow.svg`（SVG）

## 1. フロー図

```mermaid
flowchart TD
    classDef auto fill:#DDEBF7,stroke:#2F5597,color:#000
    classDef gray fill:#EDEDED,stroke:#7F7F7F,color:#000
    classDef human fill:#FFF2CC,stroke:#BF9000,color:#000
    classDef ok fill:#E2EFDA,stroke:#548235,color:#000
    classDef ng fill:#FCE4D6,stroke:#C55A11,color:#000
    classDef rule fill:#FFFFFF,stroke:#C00000,stroke-width:2px,color:#C00000

    A["月次判定（観点 A〜E、機械）"]:::auto --> B{"機械判定の結果"}:::auto
    B -->|"T1 構造的孤立<br/>T2 休眠（確度 0.5 以上）"| C["候補として通知<br/>通常フロー：隔離 → 猶予 30 日 → 削除（人が実施）"]:::human
    B -->|"T5 証拠不足<br/>（unknown が 2 観点以上）"| D["保留：通知せず翌月に再判定"]:::auto
    B -->|"機械では判断できない範囲"| G["グレー判定<br/>人の確認が必要な候補として分類し、通常フローと分けて扱う"]:::gray

    subgraph GZ["グレーの分類（機械が判断できない理由）"]
        direction TB
        G1["G1 用途が不明<br/>休眠か待機か：DR、季節・年次バッチ、<br/>法令保管、一時的な検証環境<br/>（#2 #10 #12）"]:::gray
        G2["G2 責任者が不明<br/>Owner 不在・未タグ・引き継ぎ不明・関心喪失<br/>（#5 #6 #11）"]:::gray
        G3["G3 PJ の状態が不明<br/>終了か一時停止か、台帳なし<br/>（#1）"]:::gray
        G4["G4 依存関係が不明<br/>共有基盤、外部・他アカウント参照、<br/>無主で稼働中のジョブの消費者<br/>（#3 #4）"]:::gray
        G5["G5 利用実態が不明<br/>ボット流入、常駐接続、背景 IO で<br/>使用中に見える<br/>（#9）"]:::gray
        G6["G6 削除影響が不明<br/>データ保持義務、復元可能性<br/>（#7）"]:::gray
        G7["G7 対象外のタイプ<br/>列挙元（RE／CUR／CloudTrail）の差分<br/>（#8）"]:::gray
    end
    G --> G1 & G2 & G3 & G4 & G5 & G6 & G7

    subgraph HR["ヒアリング（人。チケットで記録）"]
        direction TB
        H0["宛先：Owner → スタック Owner → アカウント Owner → 受け皿チャネル<br/>期限：30 日。60 日でリマインド、90 日で上長へエスカレーション"]:::human
        H1["用途、待機理由、再開予定日"]:::human
        H2["後任・引き継ぎ先、Owner タグの更新"]:::human
        H3["PJ の状態と終了日、台帳への登録"]:::human
        H4["依存先・消費者の有無、上流ジョブの要否"]:::human
        H5["実利用の有無（アクセス元の確認）"]:::human
        H6["保持義務、バックアップ・復元手段"]:::human
        H7["プラットフォーム担当へ棚卸し依頼"]:::human
    end
    G1 --> H1
    G2 --> H2
    G3 --> H3
    G4 --> H4
    G5 --> H5
    G6 --> H6
    G7 --> H7

    H1 & H2 & H3 & H4 & H5 & H6 & H7 --> DEC{"削除可否（人が判定）"}:::human

    subgraph DC["判定結果と処置（すべて人が実施）"]
        direction TB
        R1["削除可<br/>バックアップ → 隔離 → 猶予 30 日 → 削除"]:::ok
        R2["条件付き削除<br/>スナップショット取得・Glacier 移行・<br/>ライフサイクル設定の上で削除"]:::ok
        R3["削除不可（使用中・待機）<br/>期限付き除外（理由コード、上限 365 日）<br/>→ 期限切れで再判定"]:::ng
        R4["引き継ぎ（必要だが無主）<br/>Owner タグ更新 → 通常監視に戻す"]:::ng
        R5["回答なし<br/>上長へエスカレーション、隔離を提案<br/>→ 回答が得られるまで削除しない"]:::gray
    end
    DEC -->|"不要と確認"| R1
    DEC -->|"データは要るが<br/>リソースは不要"| R2
    DEC -->|"使用中・待機<br/>（理由と期限あり）"| R3
    DEC -->|"必要だが<br/>責任者不在"| R4
    DEC -->|"期限までに<br/>回答なし"| R5

    RULE["不変条件：どの経路でも<br/>自動停止・自動削除は行わない。<br/>停止・隔離・削除は人が承認し、人が実行する"]:::rule
    DC ~~~ RULE
```

## 2. グレーの分類と対応表

| 分類 | 機械が判断できない理由（§3 の事象） | ヒアリング先 | 聞くこと | 想定される回答 → 判定結果 |
|---|---|---|---|---|
| G1 用途が不明 | #2 休眠と待機の区別（DR、季節・年次バッチ、法令保管）／#10 年次バッチ／#12 一時的にアイドルな正当リソース | Owner（不在ならスタック → アカウント Owner） | 用途、待機の理由、再開予定日、保持期限 | 「不要」→ 削除可／「待機中で再開日あり」→ 期限付き除外（SEASONAL／DR_STANDBY 等）／「検証済みで不要」→ 削除可 |
| G2 責任者が不明 | #5 Owner の意味解決（後任、組織改編、委託先）／#6 関心を失った Owner／#11 タグ未付与既存リソースの Owner | 推定 Owner（作成者、スタック、アカウント）→ 受け皿チャネル | 後任・引き継ぎ先、Owner タグの更新可否 | 「引き継ぐ」→ Owner タグ更新して通常監視へ／「誰も要らない」→ 削除可／回答なし → エスカレーション |
| G3 PJ の状態が不明 | #1 PJ が本当に終了したか（台帳なし、一時停止との区別） | PJ 責任者、PMO | PJ の状態（終了／一時停止／継続）と終了日、台帳への登録 | 「終了」→ 一式を削除候補（隔離 → 猶予 → 削除）／「一時停止」→ 期限付き除外／「継続」→ Owner 更新 |
| G4 依存関係が不明 | #3 共有・基盤リソースの孤立／#4 無主だが稼働中のジョブの消費者 | プラットフォーム担当、上流ジョブの Owner | 依存先・消費者の有無、外部・他アカウント参照、上流ジョブの要否 | 「依存なし」→ 削除可／「依存あり」→ 除外（SHARED_PLATFORM、年次レビュー）／「産出物に消費者なし」→ 上流ジョブごと削除候補 |
| G5 利用実態が不明 | #9 ボット流入、常駐コネクション、OS の背景 IO | Owner、アプリ担当 | 実利用の有無、アクセス元（人・システム・ボット） | 「実利用なし」→ 削除可／「実利用あり」→ 通常監視へ戻し閾値を調整 |
| G6 削除影響が不明 | #7 削除可否と影響範囲（データ保持義務、復元可能性） | Owner、データ所管、法務・監査担当 | 保持義務の有無と期限、バックアップ・復元手段 | 「保持義務あり」→ 条件付き削除（スナップショット・Glacier 移行後）または除外（COMPLIANCE_HOLD）／「義務なし」→ 削除可 |
| G7 対象外のタイプ | #8 列挙元（Resource Explorer／CUR／CloudTrail）のカバレッジ外 | プラットフォーム担当 | 差分リソースの棚卸し、対応タイプ一覧の更新 | 棚卸し後に G1〜G6 のいずれかへ振り分け |

## 3. 流れの要点

- **グレー判定**：機械判定（観点 A〜E）で T1／T2 に確定せず、かつ証拠不足（T5）でもない候補を、人の確認が必要なものとして G1〜G7 に分類し、通常フローとは分けて扱う。
- **ヒアリング**：宛先は Owner → スタック Owner → アカウント Owner → 受け皿チャネルの順で解決し、チケットで記録する。期限は 30 日、60 日でリマインド、90 日で上長へエスカレーション。
- **削除可否**：回答に応じて「削除可」「条件付き削除」「削除不可（期限付き除外）」「引き継ぎ（Owner 更新）」「回答なし（エスカレーション）」の 5 つに落とす。削除に至る経路は必ず「バックアップ → 隔離 → 猶予 30 日 → 削除」を人が実施する。回答が得られないものは削除しない。
- **除外は期限付き**：除外に落ちたものは理由コードと期限（上限 365 日）を必ず持ち、期限切れで機械判定に戻る。
